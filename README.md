# Screenwriter

An agentic AI tool for drafting and reviewing screenplay dialogue, scene by scene.

Monorepo containing two apps, run as separate processes:
- `server/` — Node/TypeScript backend using the Claude Agent SDK
- `client/` — React + Vite frontend
